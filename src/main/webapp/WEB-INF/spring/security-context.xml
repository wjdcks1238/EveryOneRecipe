<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:s="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">
	<!-- intercept-url의 access의 값이 false이면 access="USER"로 사용해야함 -->
	<!-- csrf 방어사용으로 로그아웃은 POST 방식으로 해야함 -->
	<s:http>
		<s:intercept-url pattern="/login/loginForm.do" access="permitAll" />
		<s:intercept-url pattern="/home.do" access="permitAll" />
		<s:intercept-url pattern="/admin/**" access="hasRole('ADMIN')" />
		<s:intercept-url pattern="/**" access="hasAnyRole('USER, ADMIN')" />
		
		<s:form-login/>
		<s:access-denied-handler error-page="/login/accessDenied.do" />
	</s:http>

	<!--  provider  -->
	<s:authentication-manager>
		<s:authentication-provider>
			<s:jdbc-user-service
				data-source-ref="dataSource"
				role-prefix=""
          	    users-by-username-query="select userid, password, enabled from members where userid = ?"
            	authorities-by-username-query="select userid authority from members where userid = ?"
            	group-authorities-by-username-query="SELECT userid, authority FROM MEMBERS WHERE authority ='A'"
			/>
		</s:authentication-provider>
	</s:authentication-manager>	
</beans>